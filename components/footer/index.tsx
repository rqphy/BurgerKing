import React from "react"
import s from "./styles.module.scss"
import cn from "classnames"

export type IProps = {

}

export default function Footer (props: IProps) {
    return (
        <footer className={s.footer}>Hello World</footer>
    )
}
