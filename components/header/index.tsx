import React from "react"
import s from "./styles.module.scss"
import cn from "classnames"

export type IProps = {

}

export default function Header (props: IProps) {
    return (
        <header className={s.header}>Hello World</header>
    )
}
